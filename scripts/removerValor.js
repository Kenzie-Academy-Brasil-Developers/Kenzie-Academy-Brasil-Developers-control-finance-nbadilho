let botaoRemover=document.querySelectorAll("[data-botao-lixeira]")
let arrRemover=[...botaoRemover]


function removerValor(valor) {
    insertedValues = insertedValues.filter(obj => obj !== valor[0])
}

